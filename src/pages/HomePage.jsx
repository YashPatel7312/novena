import React from "react";
import HeroHome from "../components/HeroHome";  // ✅ your Hero component     // ✅ CSS file (no default import)

const HomePage = () => {
  return (
    <>
      <HeroHome />
    </>
  );
};

export default HomePage;
